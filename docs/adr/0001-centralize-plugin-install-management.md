# 插件实例安装统一从插件广场管理

实例安装清单只用于跨插件、跨 OpenClaw 实例观察安装状态和错误，不提供管理入口。安装新版本从具体插件版本发起，已有实例安装的启用、配置、Agent 范围、更新、重新安装和卸载统一从插件广场中的插件入口进入，以避免同一能力分散在两个页面并混淆插件版本与实例安装的边界。

当插件库中已经没有可用版本但实例安装仍然存在时，插件广场仍展示该插件，使仍受后端支持的实例操作保持可访问。

已下架插件明确提示能力限制，并隐藏依赖可用插件版本的更新、重新安装和配置操作；启用状态、Agent 范围和卸载等只依赖实例安装记录的操作仍然可用。
